
export type categoryProjects = {
  title: string;
  description: string;
  imageUrl: string;
  projectUrl: string;
  technologies?: string[];
};

export type aboutMe = {
  codeString: string;
};

export type skill = {
  name: string;
  imageUrl: string;
};

export type milestone = {
  date: string;
  title: string;
  description: string;
  icon: string;
  technologies?: string[];
};

